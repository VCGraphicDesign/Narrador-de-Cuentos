{
    "functions": {
        "api/*.ts": {
            "runtime": "vercel-node@latest"
        }
    },
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/$1"
        }
    ]
}